schemaVersion: scg.config/v1alpha1

sources:
  - root: examples/mixed-consumer/services/native
    inputSchema: scg-v1
  - root: examples/mixed-consumer/services/zdp
    inputSchema: zdp-v2

scan:
  exclude:
    - .git/**
    - node_modules/**
    - .catalog/**

validation:
  failOnWarnings: true
  allowUnknownDependencies: false
  staleAfterDays: 365
  minimumServiceCount: 2

output:
  directory: .catalog-mixed
  formats:
    - json
    - dot
    - html
